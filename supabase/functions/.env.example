# Secrets for the Drifterr edge functions. Set them in production with:
#   supabase secrets set --env-file supabase/functions/.env
# (SUPABASE_URL / SUPABASE_ANON_KEY / SUPABASE_SERVICE_ROLE_KEY are injected
#  automatically by the platform — you do NOT set those here.)
#
# The Stripe *price* ids are NOT secret and live in the database (the `plans`
# table, seeded by supabase/seed.sql), so they are not listed here.

# Where the user-facing site lives (used for Checkout success/cancel + CORS).
SITE_URL=https://drifterr.app

# Stripe — from the Stripe dashboard (use test keys while developing).
STRIPE_SECRET_KEY=sk_test_...
# After creating the webhook endpoint (see README), paste its signing secret:
STRIPE_WEBHOOK_SECRET=whsec_...

# Ed25519 seed (32 bytes, base64url) used to sign the plan assertion /me hands the
# desktop app. Its public half is compiled into release builds of the app as
# DRIFTERR_ENTITLEMENT_PUBKEY, and the proxy verifies against it instead of taking
# the panel's word for which plan the user has.
#
# Generate a pair — see supabase/functions/_shared/plan_token.ts for the one-liner.
# Unset means /me sends no token and the app reports the plan as unverified, which
# is the honest state rather than a silent downgrade.
# ENTITLEMENT_SIGNING_KEY=
