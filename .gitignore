/target
**/*.rs.bk
Cargo.lock.orig
*.sqlite
*.sqlite-journal
*.db
.DS_Store

# Local env (secrets / machine config) — keep .env.example tracked
.env

# Node / Playwright
node_modules/

# Tauri build output
apps/desktop/src-tauri/target/
apps/desktop/src-tauri/gen/

# Supabase CLI local state (keep config.toml, migrations, functions tracked)
supabase/.branches/
supabase/.temp/

# Bundled semantic model (fetched on demand — never committed)
apps/desktop/src-tauri/models/

# Built browser-extension package (regenerated by scripts/package.sh)
apps/extension/*.zip
