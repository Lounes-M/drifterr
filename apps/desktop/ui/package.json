{
  "name": "drifterr-menubar-ui",
  "version": "0.0.1",
  "private": true,
  "description": "Drifterr menubar panel — static assets + headless UI tests.",
  "type": "module",
  "scripts": {
    "test": "node tests/config.test.mjs && node tests/menubar.test.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
